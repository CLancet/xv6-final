1.RISC-V的函数调用过程参数优先使用寄存器传递，即a0~a7共8个寄存器。返回值可以放在a0和a1寄存器。printf的参数13保存在a2寄存器。
2.没有这样的汇编代码，g函数被内联到f函数中，f函数又被内联到main函数中。
3.通过main函数中第34行可知，入口地址为628,`0x0000000000000628`。
4.0x38
5.应该将i赋值为0x726c6400,而57616不需要，57616十六进制是110，无论端序如何。
6.y的值应该就是当前寄存器的值
